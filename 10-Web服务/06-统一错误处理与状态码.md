# 统一错误处理与状态码

Web 服务里最容易混乱的地方之一是错误处理。每个 handler 自己决定状态码，时间久了会出现同一种错误返回不同状态码的情况。

这一篇建立统一错误模型。

---

## 1. 业务错误和 HTTP 错误分开

service 层可以定义业务错误：

```go
package task

import "errors"

var (
	ErrNotFound      = errors.New("task not found")
	ErrAlreadyDone   = errors.New("task already done")
	ErrInvalidStatus = errors.New("invalid task status")
)
```

这些错误不包含 HTTP 状态码，因为 service 不应该依赖 HTTP 协议。

---

## 2. 在 HTTP 边界映射状态码

handler 或 `httpx` 包负责映射：

```go
package task

import (
	"errors"
	"net/http"

	"example.com/task-api/internal/httpx"
)

func writeTaskError(w http.ResponseWriter, err error) {
	switch {
	case errors.Is(err, ErrNotFound):
		httpx.WriteErrorWithCode(w, http.StatusNotFound, "task_not_found", "task not found")
	case errors.Is(err, ErrAlreadyDone):
		httpx.WriteErrorWithCode(w, http.StatusConflict, "task_already_done", "task already done")
	case errors.Is(err, ErrInvalidStatus):
		httpx.WriteErrorWithCode(w, http.StatusUnprocessableEntity, "invalid_status", "invalid task status")
	default:
		httpx.WriteErrorWithCode(w, http.StatusInternalServerError, "internal_error", "internal server error")
	}
}
```

这样 service 可以继续保持协议无关，HTTP 层也能稳定控制返回格式。

---

## 3. 定义 AppError

如果错误种类很多，可以定义统一类型：

```go
package httpx

import (
	"fmt"
)

type AppError struct {
	Status  int
	Code    string
	Message string
	Err     error
}

func (e *AppError) Error() string {
	if e.Err == nil {
		return e.Message
	}
	return fmt.Sprintf("%s: %v", e.Message, e.Err)
}

func (e *AppError) Unwrap() error {
	return e.Err
}
```

使用它包装错误：

```go
return &httpx.AppError{
	Status:  http.StatusBadRequest,
	Code:    "invalid_json",
	Message: "invalid json body",
	Err:     err,
}
```

对外返回 `Message`，日志记录完整 `Err`。这样既能给客户端清楚信息，也不会把内部错误细节暴露出去。

---

## 4. 让 handler 返回 error

标准库 handler 没有返回值，但可以自己包一层：

```go
package httpx

import (
	"errors"
	"log/slog"
	"net/http"
)

type HandlerFunc func(http.ResponseWriter, *http.Request) error

func Wrap(logger *slog.Logger, fn HandlerFunc) http.HandlerFunc {
	return func(w http.ResponseWriter, r *http.Request) {
		if err := fn(w, r); err != nil {
			WriteMappedError(logger, w, r, err)
		}
	}
}

func WriteMappedError(logger *slog.Logger, w http.ResponseWriter, r *http.Request, err error) {
	var appErr *AppError
	if errors.As(err, &appErr) {
		WriteErrorWithCode(w, appErr.Status, appErr.Code, appErr.Message)
		return
	}

	logger.Error(
		"unhandled request error",
		slog.String("method", r.Method),
		slog.String("path", r.URL.Path),
		slog.String("error", err.Error()),
	)
	WriteErrorWithCode(w, http.StatusInternalServerError, "internal_error", "internal server error")
}

func WriteErrorWithCode(w http.ResponseWriter, status int, code string, message string) {
	body := ErrorResponse{
		Error: ErrorBody{
			Code:    code,
			Message: message,
		},
	}
	if err := WriteJSON(w, status, body); err != nil {
		http.Error(w, http.StatusText(http.StatusInternalServerError), http.StatusInternalServerError)
	}
}
```

业务 handler 可以写得更线性：

```go
func (h *HTTPHandler) Create(w http.ResponseWriter, r *http.Request) error {
	var req createTaskRequest
	if err := httpx.DecodeJSON(w, r, &req, 1<<20); err != nil {
		return &httpx.AppError{
			Status:  http.StatusBadRequest,
			Code:    "invalid_json",
			Message: "invalid json body",
			Err:     err,
		}
	}

	if err := req.Validate(); err != nil {
		return &httpx.AppError{
			Status:  http.StatusUnprocessableEntity,
			Code:    "invalid_request",
			Message: err.Error(),
			Err:     err,
		}
	}

	task, err := h.service.Create(r.Context(), req.ToInput())
	if err != nil {
		return mapTaskError(err)
	}

	return httpx.WriteJSON(w, http.StatusCreated, taskResponseFromModel(task))
}
```

---

## 5. 错误码要稳定

错误响应建议包含机器可读的 `code`：

```json
{
  "error": {
    "code": "task_not_found",
    "message": "task not found"
  }
}
```

`message` 可以更自然，`code` 应该稳定。前端、客户端 SDK、测试用例更适合依赖 `code`。

---

## 6. 错误日志不要重复打

一个常见问题是每层都记录同一个错误。更推荐：

- service 返回错误，不记录日志。
- repository 返回错误并保留上下文，通常不记录日志。
- HTTP 最外层统一记录未处理错误。
- 预期业务错误可以不记 error 级别日志。

包装错误时保留上下文：

```go
task, err := repo.FindByID(ctx, id)
if err != nil {
	return Task{}, fmt.Errorf("find task by id %q: %w", id, err)
}
```

最后映射时仍然可以通过 `errors.Is` 找到根因。

---

## 7. 学习检查

检查错误处理时，重点看：

1. service 是否返回业务错误。
2. HTTP 状态码是否集中映射。
3. 错误响应是否有固定 JSON 结构。
4. 预期错误和未知错误是否区分。
5. 对外 message 是否避免泄露内部细节。
6. 日志是否包含 method、path、错误详情。
7. 错误是否用 `%w` 包装，方便 `errors.Is` 和 `errors.As`。
