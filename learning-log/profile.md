# Learning Profile

## 当前画像

- 学习目标：系统学习 Go，能讲清概念、写出最小代码、用命令验证结果。
- 教学偏好：用户自己读文档、卡住来问；先讲后问——讲解直接给，讲完出 1 个小检查题防划水（答错直接讲，不憋答案）；看不懂的文章段落讲懂后回写进文档；验证命令作为建议给出，不强制执行。
- 当前水平：02 基础类型与控制流大部分核心点已覆盖；剩余 10 综合练习待学。
- 已完成主题:值类型和字面量;零值;var 声明与类型推断;`:=` 短声明;作用域与变量遮蔽;赋值;常量与 iota;数字类型与浮点不精确;字符串、byte 与 rune;显式类型转换与 `string(byte)` vs `strconv.Itoa`;布尔表达式与 if 必须是 bool;if 初始化语句(err-handling 惯用法);for 循环与 range(三种基本形式 + `_` 占位 + range 值是副本 + range string 字节索引跳号 + range map 顺序不保证 + 练习 `sum`/`multiply`);switch 基础(默认不贯穿 + 多值 case + 无表达式 switch)+ grade 练习;**slice 概念模型(sliceHeader struct + 含指针字段的 Box 类比 + `d := a` / `d = append` 两个时刻图 + Go vs JS 值类型/引用类型对照)**;nil slice / 空 slice 区分(对外 JSON 输出);切片表达式开窗不复制;cap 从窗口起点数到底层数组末尾;切片后 len 与 cap(空 slice 与 nil slice len=0 但 `s == nil` 不同)。
- 当前薄弱点:~~容易把值类型/引用类型和栈/堆分配混在一起(保留到 04 章节)~~ → **进展中**(2026-07-17 已讲清 slice 头是"值类型 + 含指针字段"两个事实并存,但口诀化使用还需要再练;栈/堆那一层仍未单独讲);**用 JS 思维理解 slice 变量**(JS 数组是引用类型,用户默认"赋值 = 共享同一对象身份",Go 里 slice 变量是独立 struct + data 字段值碰巧相同,要主动洗掉 JS 印象);range 索引字节/字符混淆(2026-06-25 再次练习,预测基本正确但描述绕,再观察);`v *= 10` 容易跟指针混淆(已讲过复合赋值是 `v = v * 10`,与指针无关,下次再观察);**if 初始化语句 `if x := ...; cond {}` 反复遗忘**(2026-07-16 复习时在 02-02 和 02-07 两处都没认出来,已在两篇文章补充讲解 + "for 循环第一段"记忆锚点,06 模块 err-handling 高频使用后应自然巩固);**`fmt.Print*` 用法混淆** —— `Println` 不解读格式化动词,要看类型必须 `Printf("%T", x)`,2026-07-17 练习时踩过。
- 当前模块:**04 数据结构与内存语义(04-04 map 基础 进行中)** —— 用户感觉 slice 坑太多,主动决定**跳过 04-02 剩余(08/10/11/练习) + 04-03 整章(slice 共享 append 与删除)**,直接进 map 基础;slice 后续回头补。
- 下一节目标:走完 04-04(map 基础);之后是 04-05(map 内存语义/排序/并发边界);**04-02 剩余 + 04-03 整章**单独标记为延后,等用户信心恢复后回头补;02-10 综合练习继续延后。
- 验证习惯：已用 `go run` 验证 `len`、索引、`range`、`string(byte)` vs `strconv.Itoa`、类型不匹配修复、`if n{}` 编译错误、`canRegister` 函数、`strconv.Atoi` + if init、range 三种形式、`v *= 10` 改副本不改原 slice、range string 字节跳号 `0 1 2 5`、练习 `sum: 6` + `after multiply: [10 20 30]`、switch 多值 case 输出 `weekend`、grade 函数 7 个分数全对；后续继续用 `go run` / `go test` 验证。

## Agent 约定

- 开始学习前先读本文件，从"当前模块 / 下一节目标"继续。
- 先讲后问：直接讲解，不用诊断题拦在讲解前；讲完出 1 个小检查题收口，答错直接讲清楚。
- 用户看不懂的文章段落，讲懂后直接回写进文档。
- 一次回复可以推进多个知识点，节奏以讲清楚为准。
- 复习时按日志先串讲一遍（概念 + 当时的易错点），用户说哪里模糊再展开；薄弱点上可出 1-2 个小检查题。
- 学习小节结束时直接把日志写入 `learning-log/daily/YYYY-MM-DD.md` 并更新本文件，写完告知用户即可。
