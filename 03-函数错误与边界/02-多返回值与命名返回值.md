# 02: 多返回值与命名返回值

Go 函数可以返回多个值。最常见的模式是返回结果和错误：

```go
value, err := doSomething()
```

---

## 1. 多返回值

```go
func divide(a, b int) (int, int) {
    return a / b, a % b
}
```

调用：

```go
func main() {
    quotient, remainder := divide(10, 3)
    fmt.Println(quotient, remainder)
}
```

多返回值让 Go 代码可以直接表达“一个操作产生多个结果”。

---

## 2. 返回结果和错误

```go
func safeDivide(a, b int) (int, error) {
    if b == 0 {
        return 0, fmt.Errorf("除数不能为 0")
    }
    return a / b, nil
}
```

调用方必须检查错误：

```go
result, err := safeDivide(10, 0)
if err != nil {
    fmt.Println("error:", err)
    return
}
fmt.Println(result)
```

这是 Go 最核心的错误处理习惯。

---

## 3. 忽略不需要的返回值

如果确实不需要某个返回值，可以用 `_`。

```go
quotient, _ := divide(10, 3)
fmt.Println(quotient)
```

但不要随便忽略 `error`：

```go
// result, _ := safeDivide(10, 0) // 不推荐
```

---

## 4. 命名返回值

返回值可以命名：

```go
func calc(a, b int) (sum int, product int) {
    sum = a + b
    product = a * b
    return
}
```

第一眼看不懂很正常，这里有两个陌生动作。拆开看：

### 陌生点 ①：签名里的 `(sum int, product int)` 是在"预先声明变量"

普通写法 `(int, int)` 只说了"返回两个 int"。命名写法等于**在函数最开头偷偷帮你声明好了两个变量**，可以脑补成：

```go
func calc(a, b int) (int, int) {
    var sum int       // ← 签名里的命名，效果等于这两行
    var product int   //   一进函数就存在，且已是零值 0
    ...
}
```

所以函数体里写的是 `sum = a + b`（赋值 `=`），**不是** `sum := a + b`——变量已经被签名声明过了，再用 `:=` 反而会出问题。

用打印验证"一进函数就存在且为零值"：

```go
func calc(a, b int) (sum int, product int) {
    fmt.Println("刚进函数时:", sum, product) // 输出：刚进函数时: 0 0
    sum = a + b
    product = a * b
    return
}
```

### 陌生点 ②：光秃秃的 `return` 是缩写

因为返回值已经有名字了，`return` 不带任何东西时，Go 自动理解为"把当前的 sum 和 product 返回出去"：

```go
return            // 等价于 ↓
return sum, product
```

这叫**裸返回（naked return）**。所以整个函数的执行流程是：

```text
调用 calc(3, 4)
  → sum=0, product=0（签名声明，零值）
  → sum = 7
  → product = 12
  → return（= return sum, product）
  → 调用方拿到 7, 12
```

### 零值直接可用

正因为命名返回值一开始就是零值，这样的函数也合法：

```go
func defaultValue() (n int) {
    return
}
```

`defaultValue()` 返回 `0`——n 声明了、没赋值、裸返回，返回的就是零值。

---

## 5. 什么时候使用命名返回值

命名返回值适合：

- 返回值含义不明显，需要名字说明。
- 函数很短，裸 `return` 不会降低可读性。
- 配合 `defer` 修改返回错误。

不适合：

- 函数很长。
- 返回值很多。
- 裸 `return` 会让读者不知道返回了什么。

更清晰的写法通常是显式返回：

```go
func calc(a, b int) (int, int) {
    sum := a + b
    product := a * b
    return sum, product
}
```

---

## 练习

实现函数：

```go
func divmod(a, b int) (int, int, error)
```

要求：

1. 返回商、余数和错误。
2. `b == 0` 时返回错误。
3. 正常情况返回 `nil` 错误。
